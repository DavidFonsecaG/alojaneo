{
  "name": "hotel-reservation-system",
  "private": true,
  "scripts": {
    "dev:api": "pnpm --filter @hrs/api dev",
    "test": "pnpm --filter @hrs/api test",
    "db:migrate": "pnpm --filter @hrs/db migrate"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
